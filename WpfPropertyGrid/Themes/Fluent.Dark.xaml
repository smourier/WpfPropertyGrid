<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:pg="clr-namespace:WpfPropertyGrid">

    <BooleanToVisibilityConverter x:Key="BoolToVis" />
    <SolidColorBrush x:Key="CategoryExpanderBackgroundBrush" Color="Blue" />

    <Style x:Key="{ComponentResourceKey {x:Type pg:PropertyGrid}, CategoryGroupStyle}" TargetType="{x:Type GroupItem}">
        <Setter Property="Margin" Value="0,0,0,5" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type GroupItem}">
                    <Expander
                        x:Name="GroupItem"
                        Padding="2"
                        IsExpanded="True">
                        <Expander.Header>
                            <DockPanel>
                                <TextBlock
                                    Margin="5,0,0,0"
                                    Background="{StaticResource CategoryExpanderBackgroundBrush}"
                                    FontWeight="Bold"
                                    Text="{Binding Path=Name}" />
                            </DockPanel>
                        </Expander.Header>
                        <Expander.Content>
                            <!--  seems to be needed for .NET 9 theme mode  -->
                            <ItemsPresenter Visibility="{Binding IsExpanded, ElementName=GroupItem, Converter={StaticResource BoolToVis}}" />
                        </Expander.Content>
                    </Expander>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>